BASENAME = $(GTK_LIB_DIR)Gtk$(DIRSEP)

CFLAGS += $(shell pkg-config --cflags gtk+-2.0)

foreach(FILE, $(rootname $(basename $(glob $(SRC_DIR)Gtk/Gtk/*.c))))
	RivaProgram($(BASENAME)$(FILE), $(FILE))
	GtkGtk: $(BASENAME)$(FILE).riva
	export

.DEFAULT: GtkGtk
