BASENAME = $(LIB_DIR)Math$(DIRSEP)

CFLAGS += -std=gnu99
LDFLAGS += Std.rlib Riva.rlib

RivaProgram($(BASENAME)Matrix, Matrix)
RivaProgram($(BASENAME)Methods, Methods)

Math:\
	$(BASENAME)Matrix.riva\
	$(BASENAME)Methods.riva

.SUBDIRS:\
	Polynomial

.DEFAULT: Math
